const API_ROOT_PATH = "/api"
const API_VERSION_PATH = "/v1"

const AUTH = {
  ROOT_PATH: '/auth',
  INIT_PATH: '/init',
  LOGIN_PATH: '/login',
  REFRESH_TOKEN_PATH: '/refresh-token',
}

const ADMIN = {
  ROOT_PATH: '/admin',
}

const OWNER = {
  ROOT_PATH: '/owner',
}

const COUNTERMAN = {
  ROOT_PATH: '/counterman',
}

const AGENT = {
  ROOT_PATH: '/agent',
}

const PUBLIC = {
  ROOT_PATH: '/public',
}

module.exports = {
  API_ROOT_PATH, API_VERSION_PATH, AUTH, ADMIN, OWNER, COUNTERMAN, AGENT, PUBLIC
}